Class Program {
    main() {
        Var sum: Int = 0;
        Foreach(i In 1 .. 100) {
            sum = sum + i;
            Foreach(j In 1 .. 100 By 1000) { sum = sum + j; }
            If (sum >= 1000) { Break; }
            Elseif (sum <= 500) { Continue; }
        }
    }
}